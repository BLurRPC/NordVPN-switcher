[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nordvpn_switcher"
version = "1.0.1"

[tool.setuptools.packages.find]
where = ["nordvpn_switcher"]
include = ["nordvpn_switcher*"]
